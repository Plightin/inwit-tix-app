# Blueprint for deploying Inwit Tix on Render using a Paid Plan and Docker

databases:
  - name: inwit-tix-db
    plan: 'starter' # Entry-level paid database plan

services:
  - name: inwit-tix-app
    type: web
    env: docker
    plan: 'starter' # Entry-level paid web service plan
    healthCheckPath: /
    # UPDATED: Use a more explicit command to run the Flask CLI
    preDeployCommand: "python -m flask db-init"
    envVars:
      - key: FLASK_APP
        value: app:app
      - key: DATABASE_URL
        fromDatabase:
          name: inwit-tix-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: UPLOAD_FOLDER
        value: /var/data/uploads # This path is for the persistent disk
    disk:
      name: uploads-disk
      mountPath: /var/data/uploads
      sizeGB: 1 # Smallest persistent disk size

